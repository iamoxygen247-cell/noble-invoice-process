[pytest]
# Collect only the offline suite in tests/. The harnesses in scripts/ match
# pytest's *_test.py pattern but import the Azure SDK and would error at
# collection; they are run by hand, not under pytest.
testpaths = tests